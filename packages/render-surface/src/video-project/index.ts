/**
 * Freeform video-editor render surface — the headless twin of the admin
 * editor's free-mode preview. Exported at the `@vismay/render-surface/video-project`
 * subpath so host apps mount the shell directly when they need it.
 */
export { default as VideoProjectShell } from './VideoProjectShell'
