// The recap markdown renderer now lives in @vismay/ui so the admin and
// footshorts/web viewers share one implementation (and embedded fs: viz
// support). Re-exported here to keep existing import paths stable.
export { RecapMarkdown } from '@vismay/ui';
