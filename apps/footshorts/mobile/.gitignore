# CNG (Continuous Native Generation): native projects are generated by
# `expo prebuild` locally and on EAS — config plugins/entitlements in
# app.config.ts are the source of truth, never hand-edited native files.
/ios
/android

# Local build artifacts
/build
.expo/
