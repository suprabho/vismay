'use client'

// Moved into @vismay/ui so the marketing grid and the admin preview share one
// aura renderer. Re-exported here for the app's existing import sites.
export { AuraBackground as default, AuraBackground } from '@vismay/ui'
